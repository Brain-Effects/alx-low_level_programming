#include <stdio.h>
#include <stdlib>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char c;
	c = 'a'
		for (c >= 'a'; c <= 'z'"\n"; c++)
{
		putchar(c);
		if (c == 'v')
			putchar('x')
}
		return (0);
}

